---
title: "What is ArcX?"
description: "ArcX wraps perp DEX strategies into a vault token that splits into separately tradeable yield and points exposure."
icon: "book-open"
---

## The Problem

Points farming on perp DEXes is capital-inefficient. You deposit capital, lock it in a strategy, earn points alongside yield, and hope for an airdrop. You can't sell the points pre-TGE, you can't separate the yield from the points, and you can't get leveraged exposure to either one independently.

## The Solution

We wrap perp DEX strategies into a **vToken** — a vault share that earns both fixed yield and exchange points. Hold it for the combined return, or use your vTokens to buy **ST** (yield) or **EPT** (points) on the orderbook.

Points farmers buy EPT for leveraged points exposure. Yield seekers buy ST at a discount for fixed returns. Each user gets exactly the exposure they want, at a price set by the market.

---

## Who is ArcX For?

<CardGroup cols={3}>
  <Card title="Points Farmer" icon="chart-line">
    Deposit \$100 for vTokens. Buy EPT on the orderbook. At \$0.02 per EPT, you end up holding **5,000 EPT** — 50x points leverage.
  </Card>
  <Card title="Yield Seeker" icon="vault">
    EPT buyers pay for the points portion of a vToken, leaving ST available below NAV. You pick it up at a discount. The vault pays **12% net APR**. You earn the yield plus the discount.
  </Card>
  <Card title="Just Hold vToken" icon="wallet">
    Earn yield and accrue points credits in one token. The simplest way to use ArcX.
  </Card>
</CardGroup>

**Exit:** Sell your ST or EPT on the orderbook to receive vTokens. Withdraw vTokens for USDC (takes up to 3 days).

---

## The Tokens

<CardGroup cols={2}>
  <Card title="vToken" icon="circle-dollar-to-slot" href="/learn/vtoken">
    The base. Vault share that earns yield + points.
  </Card>
  <Card title="Strategy Token (ST)" icon="vault" href="/learn/strategy-token">
    Yield claim. Redeems for USDC at NAV after maturity.
  </Card>
  <Card title="Expected Points Token (EPT)" icon="star" href="/learn/expected-points-token">
    Points claim. Accrues credits. Claims PointsTokens after maturity.
  </Card>
  <Card title="PointsToken" icon="coins" href="/learn/points-token">
    1:1 representation of exchange points. Burns for airdrop tokens at TGE.
  </Card>
</CardGroup>

---

## How It Works

<Steps>
  <Step title="Deposit USDC">
    Deposit USDC into a vault. You receive **vToken** shares at the current NAV. Your vToken earns fixed yield and accrues credits for points. You can stop here.
  </Step>
  <Step title="Trade (optional)">
    Use your vTokens to buy **EPT** (points leverage) or **ST** (discounted yield) on the orderbook. One orderbook, two pairs: ST/vToken and EPT/vToken. The matching engine handles the rest.
  </Step>
  <Step title="Earn">
    **Yield:** The market maker borrows vault capital and pays a fixed interest rate (e.g. 15% APR gross, 12% net after performance fee). NAV grows linearly.

    **Points:** Both vToken and EPT holders accrue credits — a time-weighted measure of your position. Each week, exchange points are distributed proportional to your credit share. Earlier holders earn more. [Credit Mathematics →](/deep-dives/credit-mathematics)
  </Step>
  <Step title="Redeem">
    At epoch maturity: ST redeems for USDC at final NAV. EPT claims PointsTokens. vToken holders can do either.

    To withdraw before maturity: sell on the orderbook, recombine to vToken if needed, then request withdrawal. Finalization takes up to 3 days.
  </Step>
</Steps>

---

## NAV and Pricing

**NAV** is the per-share USDC value of the vault. It grows linearly as the market maker pays the fixed interest rate. NAV does not respond to orderbook activity — it only reflects the vault's assets.

**ST** trades on the orderbook priced in vTokens. It represents the yield claim — at maturity, ST redeems for USDC at the final NAV. ST trades below NAV because EPT buyers are paying for the points portion of the vToken, leaving the yield portion available at a discount. The more someone pays for EPT, the cheaper ST gets. That discount is the yield seeker's opportunity.

**EPT** trades on the orderbook priced in vTokens. It represents the points claim. EPT price reflects what the market thinks the points are worth. EPT decays over the epoch as fewer credits remain to earn.

**Matching.** One orderbook handles both ST and EPT. When an ST buy and an EPT buy arrive together, the engine mints — splitting vTokens into ST + EPT. When an ST sell and an EPT sell arrive together, the engine burns — recombining into vTokens. This means an ST buy can be filled by either an ST seller or an EPT buyer, and vice versa. [EPT Pricing →](/deep-dives/ept-pricing)

---

## Fees

| Fee | Rate | Who | Applied |
|---|---|---|---|
| Performance fee | 20% of interest earned | vToken holders (deducted from NAV growth) | On yield |
| PointsToken claim fee | 25% of PointsTokens | EPT and vToken holders who claim points | On claiming |

---

## Epochs

Each strategy runs in epochs that progress through three stages:

<Steps>
  <Step title="Active">
    <Badge color="green">ACTIVE</Badge>

    Deposits open. Credits accruing. Orderbooks open for trading.
  </Step>
  <Step title="Settling">
    <Badge color="orange">SETTLING</Badge>

    Strategy unwinds. No new deposits. Orderbooks open for secondary trades only.
  </Step>
  <Step title="Matured">
    <Badge color="red">MATURED</Badge>

    Final NAV and points published. ST redeems for USDC. EPT claims PointsTokens.
  </Step>
</Steps>

Epochs are independent. No automatic rollover. See [Epoch Lifecycle](/learn/epoch-lifecycle) for details.

---

## Vaults at Launch

Each vault pairs a professional market maker with one or more perp DEXes. The market maker borrows vault capital, runs their own trading strategies, and pays back a fixed interest rate. ArcX does not dictate or operate the trading — each market maker runs their own book.

Each vault has its own vToken. ST and EPT derived from each vault are specific to that vault and epoch.
